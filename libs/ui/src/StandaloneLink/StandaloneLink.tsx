'use client'

// Using a Server Component as a direct child of `<Link legacyBehavior>` is not supported by Next.
// When Next fixes this, we can remove this component and import directly from the design system.

export { StandaloneLink } from '@amsterdam/design-system-react/dist/StandaloneLink'
