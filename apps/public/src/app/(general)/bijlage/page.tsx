import type { Metadata } from 'next'

import { Bijlage } from './Bijlage'

export const metadata: Metadata = {
  title: 'Stap 2 van 4 - Foto’s - Gemeente Amsterdam',
}

export default async () => <Bijlage />
