import type { MeldingWithAddress } from './overviewFields'

type Props = {
  meldingen: MeldingWithAddress[]
}

export const OverviewMobile = (_props: Props) => {
  // Intentionally empty for now; this will become the card-based overview.
  return null
}
